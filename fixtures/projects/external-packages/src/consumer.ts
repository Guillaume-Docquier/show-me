// oxlint-disable-next-line import/no-unassigned-import -- The fixture proves package reuse across project files.
import "@scope/package/subpath"
// oxlint-disable-next-line import/no-unassigned-import -- The fixture proves package reuse across subpaths.
import "react/feature"

export const consumerValue = true
